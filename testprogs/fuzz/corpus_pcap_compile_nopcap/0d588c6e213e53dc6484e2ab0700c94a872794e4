# Corpus для фаззинга pcap_compile_nopcap

## Обоснование выбора семплов

Функция `pcap_compile_no����` компи��ирует **строковые BPF-фильтры** в байткод. Входные данные — это текстовые выражения в синтаксисе pcap-filter, а не pсер смещенийрукапсуляцию и многослойные выражения |
| 05_ether.bin | BPF/4.txt | `ether[12:2]` �ет последний байт ка� в синтро� ко�in | BPF/5.txt | `vlan` и `ip` — тестирует VLAN-инка�.
�с