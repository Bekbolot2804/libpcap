# Corpus для фаззинга pcap_compile_nopcap

## Обоснование выбора семплов

Функция `pcap_compile_nopcap` компи��ирует **строковые BPF-фильтры** в байткод. Входные данные — это текстовые выражения в синтаксисе pcap-filter, а не pcap-файлы. Поэтому ко�in | BPF/5.txt | `vlan` и `ip` — тестирует VLAN-инкапсуляцию и многослойные выражения |
| $05.ether.bin | BPF/4.txt | `ether[12:2]` с byte-level доступом — покрывает парсер смещенийрукапсуляцию и многослойные выражения |
| 05_ether.bin | BPF/4.txt | `ether[12:2]` �ет последний байт как linktype, а остальное — как null-terminated строку фильтра.
